# v2.2 Bug 修复与功能增强 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 subagent-driven-development（推荐）或 executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 修复配置表另存为无反应、职业角色显示 bug；添加夜间模式 UI；Git 同步到 github；知识库向量块大小改为区间值并支持重新分块。

**架构：** Vue 3 + TypeScript 前端，Python FastAPI 后端，Electron 桌面壳。夜间模式通过 Tailwind `dark:` class + CSS 变量驱动。知识库重新分块在后端执行，前端触发。

**技术栈：** Vue 3 + TypeScript + Tailwind CSS + Element Plus + FastAPI + openpyxl

---

### 任务 1：修复 Bug — 配置表"另存为"无反应

**文件：**
- 修改：`src/composables/useExcel.ts:53-58`
- 修改：`src/pages/HomePage.vue:213-252`

- [ ] **步骤 1：修改 `useExcel.ts` — saveToFile 增加错误返回**

当前 `saveToFile` 在失败时返回 `null`，但调用方无法区分原因。改为抛出异常以便调用方捕获。

```typescript
// src/composables/useExcel.ts:53-58
const saveToFile = async (doc: DocRecord): Promise<string> => {
    if (!excelData.value) throw new Error('Excel 数据未加载')
    const saveRes = await axios.post(apiUrl('/api/excel/save'), { sheets: excelData.value.sheets })
    if (!saveRes.data.success) throw new Error(saveRes.data.message || 'Excel 保存接口返回失败')
    if (!saveRes.data.data_uri) throw new Error('保存后未获取到文件数据')
    return saveRes.data.data_uri
  }
```

- [ ] **步骤 2：修改 `HomePage.vue` — saveAs 流程增加完整错误处理**

替换 `handleDocCommand` 的 saveAs 分支（line 222-252），增加 loading 状态和 try/catch：

```typescript
// src/pages/HomePage.vue — 在 handleDocCommand 内
} else if (command === 'saveAs') {
    const isExcel = doc.category === 'excel' || ['xlsx', 'xls'].includes((doc.name || '').split('.').pop()?.toLowerCase() || '')
    const api = (window as any).electronAPI
    if (!api?.saveFileAs) { ElMessage.warning('仅桌面应用可用'); return }
    
    if (isExcel) {
      try {
        // 确保文档已加载且 Excel 数据就绪
        if (docs.currentDoc.value.id !== doc.id) await selectDoc(doc)
        if (!excel.excelData.value) {
          // selectDoc 可能静默失败，主动尝试加载
          await excel.loadExcelData(doc)
        }
        if (!excel.excelData.value) {
          ElMessage.warning('无法加载表格数据，请先点击该文档打开后再试')
          return
        }
        const dataUri = await excel.saveToFile(doc)
        const defaultName = (doc.name || '表格').replace(/\.[^/.]+$/, '') + '.xlsx'
        const result = await api.saveFileAs(dataUri, defaultName)
        if (result?.success) ElMessage.success('已保存至 ' + (result.filePath || ''))
        else if (result?.error && result.error !== 'Canceled') ElMessage.error('保存失败: ' + result.error)
      } catch (e: any) {
        ElMessage.error('保存失败: ' + (e?.message || String(e)))
      }
      return
    }
    // 非 Excel 文档的 saveAs（原有逻辑保持不变）
    // ...
```

- [ ] **步骤 3：验证**

启动 `npm run dev`，上传一个 xlsx 文件到配置表分类，右键 → 另存为，确认弹出保存对话框。取消和保存两种情况都测试。

---

### 任务 2：修复 Bug — 职业角色显示

**文件：**
- 修改：`src/composables/useSettings.ts:61`
- 修改：`src/pages/HomePage.vue:473`

- [ ] **步骤 1：修改 `useSettings.ts` — openSettings 加载职业数据**

```typescript
// src/composables/useSettings.ts:61-62
const openSettings = async (loadProfessionsFull: () => Promise<void>) => { 
  showSettings.value = true
  await loadProfessionsFull()
  loadConfig()
}
```

- [ ] **步骤 2：修改 `HomePage.vue` — 传递 loadProfessionsFull**

在 line 473 附近，修改 settings 按钮的点击处理：

```typescript
// 原来： const settingsBtn = () => settings.openSettings()
// 改为： 
const openSettings = () => settings.openSettings(() => prompts.loadProfessionsFull())
```

更新模板中 settings 按钮的绑定：

在 `src/pages/HomePage.vue` 的模板中（约 line 503），将 `settings.openSettings()` 替换为 `openSettings`。

- [ ] **步骤 3：验证**

打开设置 → 职业角色 tab，确认显示 策划/开发/设计/测试 四个职业。选中"策划"，确认显示默认 Prompt。

---

### 任务 3：创建主题系统（CSS 变量 + useTheme.ts）

**文件：**
- 修改：`src/style.css`
- 创建：`src/composables/useTheme.ts`
- 修改：`src/pages/HomePage.vue`
- 修改：`src/main.ts`

- [ ] **步骤 1：修改 `style.css` — 添加 CSS 变量**

```css
/* 在现有 :root 块中追加以下内容 */
:root {
  /* ... 原有 font 设置 ... */
  
  /* 主题色 — 浅色模式 */
  --app-bg: #fafafa;
  --app-surface: #ffffff;
  --app-primary: #1d4ed8;
  --app-primary-light: #eff6ff;
  --app-primary-text: #1e40af;
  --app-text: #18181b;
  --app-text-secondary: #64748b;
  --app-text-muted: #a1a1aa;
  --app-border: #e4e4e7;
  --app-border-light: #f1f5f9;
  --app-hover: #f4f4f5;
  --app-sidebar-bg: #ffffff;
  --app-editor-bg: #ffffff;
  --app-card-bg: #ffffff;
}

/* 夜间模式覆盖 */
.dark {
  --app-bg: #0f172a;
  --app-surface: #1e293b;
  --app-primary: #3b82f6;
  --app-primary-light: #1e3a5f;
  --app-primary-text: #93c5fd;
  --app-text: #e2e8f0;
  --app-text-secondary: #94a3b8;
  --app-text-muted: #64748b;
  --app-border: #334155;
  --app-border-light: #1e293b;
  --app-hover: #334155;
  --app-sidebar-bg: #1e293b;
  --app-editor-bg: #1e293b;
  --app-card-bg: #1e293b;
}
```

- [ ] **步骤 2：创建 `src/composables/useTheme.ts`**

```typescript
import { ref, watch } from 'vue'
import axios from 'axios'
import { apiUrl } from '@/utils/api'

export function useTheme() {
  const isDark = ref(false)

  // 从 localStorage 初始化
  const saved = localStorage.getItem('app-theme')
  if (saved === 'dark') {
    isDark.value = true
    document.documentElement.classList.add('dark')
  }

  const toggle = () => {
    isDark.value = !isDark.value
    apply()
    // 保存到 config.json
    saveToConfig(isDark.value)
  }

  const apply = () => {
    if (isDark.value) {
      document.documentElement.classList.add('dark')
      localStorage.setItem('app-theme', 'dark')
    } else {
      document.documentElement.classList.remove('dark')
      localStorage.setItem('app-theme', 'light')
    }
  }

  const saveToConfig = async (dark: boolean) => {
    try {
      const r = await axios.get(apiUrl('/api/config'))
      const config = r.data.success ? (r.data.data || {}) : {}
      config.darkMode = dark
      await axios.post(apiUrl('/api/config'), config)
    } catch { /* */ }
  }

  const loadFromConfig = async () => {
    try {
      const r = await axios.get(apiUrl('/api/config'))
      if (r.data.success && r.data.data?.darkMode === true) {
        isDark.value = true
        apply()
      }
    } catch { /* */ }
  }

  return { isDark, toggle, apply, loadFromConfig }
}
```

- [ ] **步骤 3：在 `HomePage.vue` 中集成 Theme**

在 `<script>` 中添加：

```typescript
import { useTheme } from '@/composables/useTheme'
const theme = useTheme()
```

在 `onMounted` 中调用 `theme.loadFromConfig()`

将来在所有模板中使用 `theme.isDark.value` 来响应主题状态。

- [ ] **步骤 4：在 `src/main.ts` 中导入 Element Plus 暗色 CSS**

```typescript
// src/main.ts — 在现有 import 之后添加
import 'element-plus/theme-chalk/dark/css-vars.css'
```

（先检查 main.ts 是否存在，如不存在则在 App.vue 或 style.css 中通过 @import 引入）

---

### 任务 4：UI 美化 — 替换 HomePage.vue 颜色类

**文件：**
- 修改：`src/pages/HomePage.vue`（模板中的颜色类）

使用 CSS 变量替换关键 Tailwind 颜色类。由于替换量较大，采用策略性替换：定义一批 `.bg-app-*` / `.text-app-*` / `.border-app-*` 工具类。

- [ ] **步骤 1：在 style.css 中添加工具类**

```css
/* 背景 */
.bg-app { background-color: var(--app-bg); }
.bg-surface { background-color: var(--app-surface); }
.bg-primary-light { background-color: var(--app-primary-light); }
.bg-sidebar { background-color: var(--app-sidebar-bg); }
.bg-editor { background-color: var(--app-editor-bg); }
.bg-card { background-color: var(--app-card-bg); }
.bg-hover { background-color: var(--app-hover); }

/* 文字 */
.text-app { color: var(--app-text); }
.text-app-secondary { color: var(--app-text-secondary); }
.text-app-muted { color: var(--app-text-muted); }
.text-app-primary { color: var(--app-primary); }

/* 边框 */
.border-app { border-color: var(--app-border); }
.border-app-light { border-color: var(--app-border-light); }

/* hover */
.hover\:bg-app-hover:hover { background-color: var(--app-hover); }
```

- [ ] **步骤 2：替换 HomePage.vue 模板中的颜色类**

替换以下关键位置（模板中，约 line 496-831）：

| 原始类 | 替换为 |
|--------|--------|
| `bg-zinc-50` | `bg-app` |
| `bg-white` | `bg-surface` |
| `bg-blue-50` | `bg-primary-light` |
| `text-zinc-900` | `text-app` |
| `text-zinc-600` | `text-app-secondary` |
| `text-zinc-400` | `text-app-muted` |
| `text-blue-700` | `text-app-primary` |
| `border-zinc-200` | `border-app` |
| `border-zinc-100` | `border-app-light` |
| `hover:bg-zinc-100` | `hover:bg-app-hover` |
| `bg-zinc-200` | `bg-app-hover`（选中态保留使用 `bg-primary-light`） |

具体替换位置示例（line 496-831 模板中）：
- `class="flex h-screen bg-zinc-50 ..."` → `flex h-screen bg-app`
- `class="w-64 border-r border-zinc-200 bg-white ..."` → `w-64 border-r border-app bg-surface`
- `class="bg-blue-50 text-blue-700"` → `bg-primary-light text-app-primary`
- 以此类推

- [ ] **步骤 3：验证**

启动 `npm run dev`，确认界面颜色正确，功能正常。

---

### 任务 5：UI 美化 — 对话框颜色替换 + 夜间模式开关

**文件：**
- 修改：`src/components/dialogs/SettingsDialog.vue`
- 修改：`src/components/dialogs/KBDialog.vue`
- 修改：`src/components/dialogs/PromptDialog.vue`
- 修改：`src/components/dialogs/ImitateDialog.vue`
- 修改：`src/pages/HomePage.vue`

- [ ] **步骤 1：SettingsDialog.vue — 添加夜间模式开关**

在通用设置 tab 中，开机自启动上方添加：

```html
<div class="flex items-center justify-between">
  <div>
    <span class="text-sm font-medium">夜间模式</span>
    <p class="text-xs text-app-muted mt-0.5">切换深色/浅色主题</p>
  </div>
  <el-switch
    :model-value="isDark"
    @change="(val: boolean) => emit('update:isDark', val)"
  />
</div>
```

Props 新增 `isDark: boolean`。
Emits 新增 `'update:isDark': [val: boolean]`。

- [ ] **步骤 2：SettingsDialog.vue — 替换颜色类**

替换 `text-zinc-700` → `text-app`、`text-zinc-500` → `text-app-secondary`、`bg-zinc-50` → `bg-primary-light`（针对表单区域）、`border-zinc-200` → `border-app` 等。

- [ ] **步骤 3：HomePage.vue — 传入 isDark 并处理**

在 SettingsDialog 绑定中添加：

```html
:is-dark="theme.isDark.value"
@update:is-dark="theme.toggle"
```

- [ ] **步骤 4：KBDialog.vue、PromptDialog.vue — 替换颜色类**

同样替换 `bg-zinc-50`、`text-zinc-400/500/600`、`border-zinc-200` 等为对应的 `bg-*`、`text-app-*`、`border-app` 类。

- [ ] **步骤 5：验证**

打开设置 → 通用设置，切换夜间模式开关，确认整个界面（包括 Element Plus 组件）切换到了深色配色。

---

### 任务 6：Git 同步到 GitHub

**文件：** 无（纯 git 命令）

- [ ] **步骤 1：初始化 Git 仓库**

```bash
cd E:/game_builder
git init
git checkout -b main
```

- [ ] **步骤 2：添加远程仓库**

```bash
git remote add origin git@github.com:NikiYaooo/GBA.git
```

- [ ] **步骤 3：首次提交**

```bash
git add .
git commit -m "feat: 游戏策划AI文档助手 v2.1 初始提交"
git push -u origin main
```

---

### 任务 7：知识库向量块大小区间值

**文件：**
- 修改：`src/composables/useKnowledgeBase.ts`
- 修改：`src/components/dialogs/KBDialog.vue`
- 修改：`api/knowledge_base.py`
- 修改：`api/routers/knowledge_base.py`

- [ ] **步骤 1：修改 `useKnowledgeBase.ts` — 添加 min/max 区间值**

```typescript
// src/composables/useKnowledgeBase.ts
const chunkSizeMin = ref(100)
const chunkSizeMax = ref(500)

// 修改 loadStats
const loadStats = async () => {
    try {
      const r = await axios.get<ApiResponse<KBStats>>(apiUrl('/api/kb/stats'))
      if (r.data.success && r.data.data) {
        kbStats.value = r.data.data
        chunkSizeMin.value = r.data.data.chunk_size_min || 100
        chunkSizeMax.value = r.data.data.chunk_size_max || 500
      }
    } catch { /* */ }
  }

// 修改 saveChunkSize
const saveChunkSize = async () => {
    const r = await axios.post(apiUrl('/api/kb/chunk-size'), { 
      min: chunkSizeMin.value, 
      max: chunkSizeMax.value 
    })
    if (r.data.success) { 
      // 保存后触发重新分块
      await rechunk()
      showChunkSizeDialog.value = false 
      await loadStats() 
    }
  }

// 新增 rechunk 方法
const rechunk = async () => {
    try {
      await axios.post(apiUrl('/api/kb/rechunk'))
      ElMessage.success('知识库已按新区间重新分块')
    } catch { ElMessage.error('重新分块失败') }
  }

// 更新 return 值
return {
    showKB, isUploadingKB, kbStats, chunkSizeMin, chunkSizeMax,
    showChunkSizeDialog, loadStats, openKB, uploadFile, 
    saveChunkSize, rechunk, deleteDocument, clearAll
  }
```

- [ ] **步骤 2：修改 `KBDialog.vue` — 区间值 UI**

```html
<!-- 替换原来的 el-dialog 内部按钮和对话框 -->
<template #header><span class="text-sm font-semibold">设置向量块大小</span></template>
<div class="space-y-4">
  <div>
    <label class="text-xs text-app-secondary block mb-1">最小值（字符数）</label>
    <el-input-number
      :model-value="chunkSizeMin" :min="50" :max="500" :step="50"
      @update:model-value="(v: number|null) => v && emit('update:chunkSizeMin', v)"
    />
  </div>
  <div>
    <label class="text-xs text-app-secondary block mb-1">最大值（字符数）</label>
    <el-input-number
      :model-value="chunkSizeMax" :min="100" :max="1000" :step="50"
      @update:model-value="(v: number|null) => v && emit('update:chunkSizeMax', v)"
    />
  </div>
  <p class="text-xs text-app-muted">修改保存后将对知识库中已有文档自动重新分块</p>
</div>
<template #footer>
  <el-button size="small" @click="emit('update:showChunkSizeDialog', false)">取消</el-button>
  <el-button size="small" type="primary" @click="emit('saveChunkSize')">保存并重新分块</el-button>
</template>
```

Props 修改：`chunkSize: number` → `chunkSizeMin: number` + `chunkSizeMax: number`
Emits 修改：`'update:chunkSize'` → `'update:chunkSizeMin'` + `'update:chunkSizeMax'`

更新 HomePage.vue 的 KBDialog 绑定：

```html
:chunk-size-min="kb.chunkSizeMin.value"
:chunk-size-max="kb.chunkSizeMax.value"
@update:chunk-size-min="(v:number) => kb.chunkSizeMin.value = v"
@update:chunk-size-max="(v:number) => kb.chunkSizeMax.value = v"
```

- [ ] **步骤 3：修改后端 `knowledge_base.py` — 区间分块 + raw_docs 存储 + rechunk**

**修改 `__init__`：**
```python
def __init__(self, data_dir: str):
    # ... 原有初始化 ...
    self._chunk_size_min = 100
    self._chunk_size_max = 500
    # 用于存储原始文档内容（支持修改 chunk size 后重新分块）
    self._raw_docs: List[Dict[str, Any]] = []
```

**修改 `_load_state` 和 `_save_state` 以持久化 raw_docs：**
```python
def _load_state(self):
    # ... 原有加载 chunks 和 vectors 代码 ...
    # 新增：加载 raw_docs
    if os.path.exists(self.chunks_path):
        try:
            with open(self.chunks_path, "r", encoding="utf-8") as f:
                data = json.load(f)
            if isinstance(data, dict):
                self._chunks = data.get("chunks", data.get("", []))
                self._raw_docs = data.get("raw_docs", [])
            else:
                self._chunks = data
                self._raw_docs = []
        except Exception:
            self._chunks = []
            self._raw_docs = []

def _save_state(self):
    data = {
        "chunks": self._chunks,
        "raw_docs": self._raw_docs
    }
    with open(self.chunks_path, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
    # ... 原有向量保存代码 ...
```

**修改 `_chunk_text_tokens` 支持区间：**
```python
import random

def _chunk_text_tokens(self, text: str, chunk_tokens: int = None, overlap_ratio: float = 0.1) -> List[str]:
    import jieba
    import random
    if chunk_tokens is not None:
        # 如果传入了固定值，使用固定值（兼容旧调用）
        pass
    else:
        # 从区间随机取
        cmin = getattr(self, '_chunk_size_min', 100)
        cmax = getattr(self, '_chunk_size_max', 500)
        chunk_tokens = random.randint(cmin, cmax)
    # ... 原有分块逻辑 ...
```

**修改 `add_document` 保存 raw_doc：**
```python
def add_document(self, file_path, filename, content, doc_type, version="v1.0", file_size=0):
    # ... 计算哈希、去重检查 ...
    
    # 保存原始文档
    file_hash = self.get_file_hash(file_path)
    raw_doc = {
        "file_hash": file_hash,
        "filename": filename,
        "content": content,
        "doc_type": doc_type,
        "file_size": file_size,
        "added_at": int(time.time()),
    }
    # 去重 raw_docs
    self._raw_docs = [d for d in self._raw_docs if d.get("file_hash") != file_hash]
    self._raw_docs.append(raw_doc)
    
    # ... 后续分块和向量化 ...
```

**新增 `rechunk_all` 方法：**
```python
def rechunk_all(self) -> Dict[str, Any]:
    """根据当前 chunk_size_min/max 重新分块所有已有文档。"""
    if not self._raw_docs:
        return {"success": False, "message": "没有原始文档数据，无法重新分块"}
    
    new_chunks = []
    new_vectors_list = []
    
    for raw in self._raw_docs:
        content = raw.get("content", "")
        if not content:
            continue
        chunks = self._chunk_text_tokens(content)
        if not chunks:
            continue
        
        file_hash = raw.get("file_hash", "unknown")
        filename = raw.get("filename", "unknown")
        doc_type = raw.get("doc_type", "unknown")
        file_size = raw.get("file_size", 0)
        
        try:
            vectors = self._encode_texts(chunks)
        except Exception as e:
            continue
        
        for i, chunk in enumerate(chunks):
            new_chunks.append({
                "id": f"{file_hash}_{i}",
                "content": chunk,
                "metadata": {
                    "filename": filename,
                    "type": doc_type,
                    "file_hash": file_hash,
                    "file_size": file_size,
                    "chunk_index": i,
                    "added_at": int(time.time()),
                },
            })
        
        if len(new_vectors_list) == 0:
            new_vectors_list = vectors
        else:
            new_vectors_list = np.vstack([new_vectors_list, vectors])
    
    import numpy as np
    self._chunks = new_chunks
    self._vectors = new_vectors_list if len(new_vectors_list) > 0 else None
    self._rebuild_bm25()
    self._save_state()
    
    return {"success": True, "message": f"已重新分块 {len(new_chunks)} 个文本块"}
```

- [ ] **步骤 4：修改 API 路由 `routers/knowledge_base.py`**

**更新 `/api/kb/chunk-size`：**
```python
@router.post("/chunk-size")
async def set_kb_chunk_size(payload: dict = Body(...)):
    cmin = int(payload.get("min", 100))
    cmax = int(payload.get("max", 500))
    if cmin < 50: cmin = 50
    if cmax > 1000: cmax = 1000
    if cmin >= cmax: cmax = cmin + 50
    
    config_path = os.path.join(get_app_data_dir(), "config.json")
    config = load_json(config_path, {})
    config["chunk_size_min"] = cmin
    config["chunk_size_max"] = cmax
    save_json(config_path, config)
    
    get_kb()._chunk_size_min = cmin
    get_kb()._chunk_size_max = cmax
    return {"success": True, "data": {"chunk_size_min": cmin, "chunk_size_max": cmax}}
```

**新增 `/api/kb/rechunk`：**
```python
@router.post("/rechunk")
async def kb_rechunk():
    try:
        result = get_kb().rechunk_all()
        return result
    except Exception as e:
        return {"success": False, "message": f"重新分块失败: {str(e)}"}
```

**更新 `/api/kb/stats` 返回 min/max：**
```python
@router.get("/stats")
async def kb_stats():
    try:
        stats = get_kb().get_stats()
        config = load_json(os.path.join(get_app_data_dir(), "config.json"), {})
        stats["chunk_size_min"] = config.get("chunk_size_min", 100)
        stats["chunk_size_max"] = config.get("chunk_size_max", 500)
        return {"success": True, "data": stats}
    except Exception as e:
        return {"success": False, "message": str(e)}
```

移除原来的 `chunk_size` 引用。

- [ ] **步骤 5：更新 `api/main.py` — 从配置加载区间值**

```python
# api/main.py — 替换原来 kb._chunk_tokens 的加载
_kb_config = load_json(CONFIG_DB, {})
kb._chunk_size_min = _kb_config.get("chunk_size_min", 100)
kb._chunk_size_max = _kb_config.get("chunk_size_max", 500)
```

- [ ] **步骤 6：更新 types/index.ts — 新增 KBStats 字段**

```typescript
// KBStats 接口增加 chunk_size_min / chunk_size_max
export interface KBStats {
  // ... 原有字段 ...
  chunk_size_min?: number
  chunk_size_max?: number
}
```

- [ ] **步骤 7：验证**

启动后端和前端，打开知识库管理，点击"向量块大小"按钮，确认显示最小/最大值两个输入框。修改后保存，确认后端重新分块成功，状态数据更新。

---

### 任务 8：验证和检查

- [ ] **步骤 1：逐一验证所有修改**

1. 配置表另存为：上传 xlsx → 右键 → 另存为 → 弹出保存对话框
2. 职业角色：设置 → 职业角色 → 4 个职业可选 → 显示对应 Prompt
3. 夜间模式：设置 → 通用设置 → 切换夜间模式 → 界面切换正确
4. Git：`git remote -v` 确认远程指向 GBA.git
5. 知识库区间：设置 100-300 → 保存 → 状态更新 → 分块数变化
